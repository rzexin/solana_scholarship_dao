/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findApplicationPda } from "../pdas";
import { SCHOLARSHIP_DAO_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CANCEL_APPLICATION_DISCRIMINATOR = new Uint8Array([
  235, 165, 42, 63, 81, 63, 93, 252,
]);

export function getCancelApplicationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_APPLICATION_DISCRIMINATOR,
  );
}

export type CancelApplicationInstruction<
  TProgram extends string = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountDao extends string | AccountMeta<string> = string,
  TAccountApplication extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountDao extends string ? ReadonlyAccount<TAccountDao> : TAccountDao,
      TAccountApplication extends string
        ? WritableAccount<TAccountApplication>
        : TAccountApplication,
      ...TRemainingAccounts,
    ]
  >;

export type CancelApplicationInstructionData = {
  discriminator: ReadonlyUint8Array;
  applicationId: bigint;
};

export type CancelApplicationInstructionDataArgs = {
  applicationId: number | bigint;
};

export function getCancelApplicationInstructionDataEncoder(): FixedSizeEncoder<CancelApplicationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["applicationId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CANCEL_APPLICATION_DISCRIMINATOR }),
  );
}

export function getCancelApplicationInstructionDataDecoder(): FixedSizeDecoder<CancelApplicationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["applicationId", getU64Decoder()],
  ]);
}

export function getCancelApplicationInstructionDataCodec(): FixedSizeCodec<
  CancelApplicationInstructionDataArgs,
  CancelApplicationInstructionData
> {
  return combineCodec(
    getCancelApplicationInstructionDataEncoder(),
    getCancelApplicationInstructionDataDecoder(),
  );
}

export type CancelApplicationAsyncInput<
  TAccountSigner extends string = string,
  TAccountDao extends string = string,
  TAccountApplication extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  dao: Address<TAccountDao>;
  application?: Address<TAccountApplication>;
  applicationId: CancelApplicationInstructionDataArgs["applicationId"];
};

export async function getCancelApplicationInstructionAsync<
  TAccountSigner extends string,
  TAccountDao extends string,
  TAccountApplication extends string,
  TProgramAddress extends Address = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
>(
  input: CancelApplicationAsyncInput<
    TAccountSigner,
    TAccountDao,
    TAccountApplication
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelApplicationInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDao,
    TAccountApplication
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SCHOLARSHIP_DAO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    dao: { value: input.dao ?? null, isWritable: false },
    application: { value: input.application ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.application.value) {
    accounts.application.value = await findApplicationPda({
      dao: expectAddress(accounts.dao.value),
      applicationId: expectSome(args.applicationId),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.dao),
      getAccountMeta(accounts.application),
    ],
    data: getCancelApplicationInstructionDataEncoder().encode(
      args as CancelApplicationInstructionDataArgs,
    ),
    programAddress,
  } as CancelApplicationInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDao,
    TAccountApplication
  >);
}

export type CancelApplicationInput<
  TAccountSigner extends string = string,
  TAccountDao extends string = string,
  TAccountApplication extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  dao: Address<TAccountDao>;
  application: Address<TAccountApplication>;
  applicationId: CancelApplicationInstructionDataArgs["applicationId"];
};

export function getCancelApplicationInstruction<
  TAccountSigner extends string,
  TAccountDao extends string,
  TAccountApplication extends string,
  TProgramAddress extends Address = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
>(
  input: CancelApplicationInput<
    TAccountSigner,
    TAccountDao,
    TAccountApplication
  >,
  config?: { programAddress?: TProgramAddress },
): CancelApplicationInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountDao,
  TAccountApplication
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SCHOLARSHIP_DAO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    dao: { value: input.dao ?? null, isWritable: false },
    application: { value: input.application ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.dao),
      getAccountMeta(accounts.application),
    ],
    data: getCancelApplicationInstructionDataEncoder().encode(
      args as CancelApplicationInstructionDataArgs,
    ),
    programAddress,
  } as CancelApplicationInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDao,
    TAccountApplication
  >);
}

export type ParsedCancelApplicationInstruction<
  TProgram extends string = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    dao: TAccountMetas[1];
    application: TAccountMetas[2];
  };
  data: CancelApplicationInstructionData;
};

export function parseCancelApplicationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelApplicationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      dao: getNextAccount(),
      application: getNextAccount(),
    },
    data: getCancelApplicationInstructionDataDecoder().decode(instruction.data),
  };
}
