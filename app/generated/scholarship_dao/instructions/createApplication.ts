/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { SCHOLARSHIP_DAO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CREATE_APPLICATION_DISCRIMINATOR = new Uint8Array([
  42, 0, 145, 164, 97, 229, 37, 235,
]);

export function getCreateApplicationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_APPLICATION_DISCRIMINATOR,
  );
}

export type CreateApplicationInstruction<
  TProgram extends string = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
  TAccountProposer extends string | AccountMeta<string> = string,
  TAccountDao extends string | AccountMeta<string> = string,
  TAccountApplication extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProposer extends string
        ? WritableSignerAccount<TAccountProposer> &
            AccountSignerMeta<TAccountProposer>
        : TAccountProposer,
      TAccountDao extends string ? WritableAccount<TAccountDao> : TAccountDao,
      TAccountApplication extends string
        ? WritableAccount<TAccountApplication>
        : TAccountApplication,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateApplicationInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  recipient: Address;
  reason: string;
  proofCid: string;
};

export type CreateApplicationInstructionDataArgs = {
  amount: number | bigint;
  recipient: Address;
  reason: string;
  proofCid: string;
};

export function getCreateApplicationInstructionDataEncoder(): Encoder<CreateApplicationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
      ["recipient", getAddressEncoder()],
      ["reason", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["proofCid", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREATE_APPLICATION_DISCRIMINATOR }),
  );
}

export function getCreateApplicationInstructionDataDecoder(): Decoder<CreateApplicationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
    ["recipient", getAddressDecoder()],
    ["reason", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["proofCid", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCreateApplicationInstructionDataCodec(): Codec<
  CreateApplicationInstructionDataArgs,
  CreateApplicationInstructionData
> {
  return combineCodec(
    getCreateApplicationInstructionDataEncoder(),
    getCreateApplicationInstructionDataDecoder(),
  );
}

export type CreateApplicationInput<
  TAccountProposer extends string = string,
  TAccountDao extends string = string,
  TAccountApplication extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  proposer: TransactionSigner<TAccountProposer>;
  dao: Address<TAccountDao>;
  application: Address<TAccountApplication>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: CreateApplicationInstructionDataArgs["amount"];
  recipient: CreateApplicationInstructionDataArgs["recipient"];
  reason: CreateApplicationInstructionDataArgs["reason"];
  proofCid: CreateApplicationInstructionDataArgs["proofCid"];
};

export function getCreateApplicationInstruction<
  TAccountProposer extends string,
  TAccountDao extends string,
  TAccountApplication extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
>(
  input: CreateApplicationInput<
    TAccountProposer,
    TAccountDao,
    TAccountApplication,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateApplicationInstruction<
  TProgramAddress,
  TAccountProposer,
  TAccountDao,
  TAccountApplication,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SCHOLARSHIP_DAO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    proposer: { value: input.proposer ?? null, isWritable: true },
    dao: { value: input.dao ?? null, isWritable: true },
    application: { value: input.application ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.proposer),
      getAccountMeta(accounts.dao),
      getAccountMeta(accounts.application),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateApplicationInstructionDataEncoder().encode(
      args as CreateApplicationInstructionDataArgs,
    ),
    programAddress,
  } as CreateApplicationInstruction<
    TProgramAddress,
    TAccountProposer,
    TAccountDao,
    TAccountApplication,
    TAccountSystemProgram
  >);
}

export type ParsedCreateApplicationInstruction<
  TProgram extends string = typeof SCHOLARSHIP_DAO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    proposer: TAccountMetas[0];
    dao: TAccountMetas[1];
    application: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateApplicationInstructionData;
};

export function parseCreateApplicationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateApplicationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      proposer: getNextAccount(),
      dao: getNextAccount(),
      application: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateApplicationInstructionDataDecoder().decode(instruction.data),
  };
}
